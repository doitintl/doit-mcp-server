{
  "name": "doit-mcp-server",
  "version": "0.14.1",
  "private": true,
  "scripts": {
    "deploy": "yarn install --frozen-lockfile && wrangler deploy",
    "dev": "wrangler dev --env local --port 8787",
    "format": "biome format --write",
    "lint:fix": "biome lint --fix",
    "start": "wrangler dev --env local --port 8787",
    "cf-typegen": "wrangler types",
    "type-check": "tsc --noEmit",
    "compile": "tsc",
    "test": "../node_modules/.bin/vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.28.0",
    "agents": "^0.0.95",
    "hono": "4.12.25",
    "jose": "^6.1.3",
    "zod": "^3.25.67"
  },
  "resolutions": {
    "hono": "4.12.25",
    "@hono/node-server": "1.19.14",
    "express-rate-limit": "8.5.2"
  },
  "devDependencies": {
    "@types/node": "^24.0.4",
    "marked": "^15.0.12",
    "typescript": "^5.8.3",
    "workers-mcp": "^0.0.13",
    "wrangler": "^4.21.2"
  }
}
