export const COMMITMENT_SORT_BY_VALUES = [
    "name",
    "startDate",
    "endDate",
    "provider",
    "createTime",
    "updateTime",
] as const;

export const COMMITMENT_SORT_ORDER_VALUES = ["asc", "desc"] as const;
